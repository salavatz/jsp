CREATE TABLE person
(
    id         INT NOT NULL AUTO_INCREMENT,
    name       VARCHAR(250),
    birth_date LONG(250)
);
CREATE TABLE user
(
    id         INT NOT NULL AUTO_INCREMENT,
    login       VARCHAR(250),
    password LONG(250)
)